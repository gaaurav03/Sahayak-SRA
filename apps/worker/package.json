{
  "name": "@sahayak/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(worker) lint not configured yet\""
  },
  "dependencies": {
    "@sahayak/core": "workspace:*",
    "bullmq": "^5.13.0",
    "dotenv": "^16.4.5",
    "ioredis": "^5.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
